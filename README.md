# TheFourth
12.9 -正在学习理解姿态解析和滤波算法

12.10 -使用简单的互补滤波，解析出了三角。P和R的稳定性不错，但是Yaw在20s内就偏移了2°，需要更高级的滤波算法。
      -对Yaw使用了低通滤波，可以让其变化精度很高了。同时也加深了对滤波算法的理解（其实是加深了对MPU6050的理解，为什么不用一个有磁力计的MPU系）

12.11 -给OLED重新写了个IIC，这样避免了雪花屏问题--IIC配置最重要的就是“时序”，因为它本质是按照手册配置合适的”时序点“进行通讯的。但是OLED和MPU6050都用的是IIC，那就必须得隔离开，分别有自己的时序。芯片是主机，外设是从机，你时序要是配置一样，那就相当于同时有人举           手，老师都不知道选哪个了，自然就雪花屏了。
      -终于有了一个可以很好展现PRY的方法。
      -完成了三Sensor的数据显示。
